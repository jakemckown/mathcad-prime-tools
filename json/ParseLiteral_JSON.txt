(:= (@FUNCTION (@LABEL VARIABLE (@ID ParseLiteral (@SUB JSON))) (@ARGS (@SEP (@LABEL VARIABLE JSON) (@LABEL VARIABLE Index)))) (@PROGRAM (@IF (@EQ (@APPLY vec2str (@ARGS (@APPLY (@LABEL FUNCTION submatrix) (@ARGS (@SEP (@SEP (@SEP (@SEP JSON Index) (+ Index 3)) 0) 0))))) "true") (@BODY (@ASSIGN (@LABEL VARIABLE Literal) 1) (@ASSIGN (@LABEL VARIABLE Index) (+ Index 4))) (@EQ (@APPLY vec2str (@ARGS (@APPLY (@LABEL FUNCTION submatrix) (@ARGS (@SEP (@SEP (@SEP (@SEP JSON Index) (+ Index 4)) 0) 0))))) "false") (@BODY (@ASSIGN (@LABEL VARIABLE Literal) 0) (@ASSIGN (@LABEL VARIABLE Index) (+ Index 5))) (@EQ (@APPLY vec2str (@ARGS (@APPLY (@LABEL FUNCTION submatrix) (@ARGS (@SEP (@SEP (@SEP (@SEP JSON Index) (+ Index 3)) 0) 0))))) "null") (@BODY (@ASSIGN (@LABEL VARIABLE Literal) NaN) (@ASSIGN (@LABEL VARIABLE Index) (+ Index 4))) (@BODY (@ASSIGN (@LABEL VARIABLE Error) "Expecting 'true', 'false', or 'null'") (@RETURN (@APPLY error (@ARGS (@APPLY (@LABEL VARIABLE (@ID ParseError (@SUB JSON))) (@ARGS (@SEP (@SEP (@LABEL VARIABLE JSON) (@LABEL VARIABLE Index)) (@LABEL VARIABLE Error))))))))) (@RETURN (@MATRIX 2 1 (@LABEL VARIABLE Literal) Index))))